<h1> Packages: </h1>


<% @categories.each do |category| %>
<p><%= category.price %>€</p>  
<%= category.number_of_visits %>
  <%= link_to category.name, gym_category_path(@gym, category.id), class: "btn btn-success" %>
<% end %>

