<div class="section-header" style="margin-top: 50px;">
  <h1 class="text-center"> <%= current_user.first_name %>'s passes</h1>
</div>
<div class="passes-cards-container">
  <%= render 'shared/my_passes' %>
</div>
 <%= render 'shared/footer' %>

